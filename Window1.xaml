<Window x:Class="WpfApp2.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="389*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Width="200" BorderThickness="0" Content="Выйти" Click="Button_Click"/>
            <Button Width="300" x:Name="redactTest" BorderThickness="0" Content="Редактировать тест" Click="redactTest_Click"/>
            <Button Width="300" BorderThickness="0" Content="Пройти тест" Click="Button_Click_1"/>
        </StackPanel>
        <Frame x:Name="frame" Grid.Row="1" NavigationUIVisibility="Hidden" Panel.ZIndex="1"/>
    </Grid>
</Window>
